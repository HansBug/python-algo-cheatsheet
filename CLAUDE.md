# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

> 说明：根目录 `AGENTS.md` 是 `CLAUDE.md` 的**软链接**，本质上是同一个文件。
> 修改规范时只改其中一份即可，不要把 `AGENTS.md` 和 `CLAUDE.md` 当成两份独立文件分别维护，否则容易造成重复修改和内容漂移。

## 项目概述

这是一个专为求职笔试和面试准备的 Python 算法模板库。所有代码都是为了让开发者快速记忆和应用而设计的，不是作为第三方库使用。

**核心特点：**

- 充分利用 Python 语言特性（列表推导、切片、内置函数等）
- 针对性能瓶颈优化，避免常见的速度陷阱
- 代码精简，方便理解和记忆
- 纯原生库实现，支持 Python 3.7-3.14
- 所有模板都经过实际题目验证

## 仓库结构

```
python-algo-cheatsheet/
├── algos/              # 算法模板存放区
│   ├── heap/          # 堆算法模板
│   │   ├── README.md          # 堆算法笔试攻略
│   │   ├── heapq_native.py    # 使用 heapq 的实现（推荐）
│   │   └── heap_manual.py     # 手动实现堆
│   └── [其他算法]/    # 未来会添加更多算法
├── tools/             # 通用工具库（非算法模板）
└── README.md          # 项目说明
```

### 目录说明

**algos/** - 算法模板核心目录

- 每个子目录代表一个算法类别（如 heap、dfs、dp 等）
- 每个算法目录包含：
    - `README.md` - 该算法的笔试攻略内容（必读）
    - 若干 `.py` 文件 - 可直接运行的算法代码示例
- **阅读顺序**：先看 README.md 了解概念和策略，再看代码实现

**tools/** - 通用工具库

- 用于支撑特定功能（如性能对比的 benchmark 等）
- 不是算法模板的一部分，读者可以忽略

## 算法模板编写规范

当添加新算法模板时，必须遵循以下规范：

### 1. 目录结构

```
algos/[算法名称]/
├── README.md           # 笔试攻略（必需）
├── [实现方式1].py      # 代码实现（至少一个）
└── [实现方式2].py      # 可选的其他实现方式
```

### 2. README.md 内容结构与写作要求

每个算法的 README.md 必须严格遵循以下结构和写作风格：

#### 标题格式

```markdown
# [算法名称]笔试攻略
```

- 使用中文标题，格式为"[算法名]笔试攻略"
- 例如："堆（Heap）笔试攻略"、"二分查找笔试攻略"

#### 根 README.md 维护要求

- 根目录 `README.md` 中的"已收录算法"表格必须按**常见程度从高到低**排序；常见程度只做粗粒度区分，同一层内再按**难度从低到高**排序
- 根目录 `README.md` 中的"计划收录的算法分类"必须使用**多级** checklist 写法：`* [ ]` / `* [x]`
- 顶层分类下面必须拆到具体算法或技巧子项，不能只保留单层分类
- 每个一级分类下面的子项，也必须按**常见程度从高到低**排序；常见程度相近时再按**难度从低到高**排序
- 父级分类只有在**所有直接子项都已完成**时，才能标记为 `* [x]`

#### 第一部分：什么是[算法]？

**目标**：让读者快速理解算法的本质和核心特性

**内容要求**：

- 用 1-2 句话给出清晰的定义
- 列出核心性质（使用无序列表）
- 如果有多种变体，简要说明区别（如最小堆 vs 最大堆）
- 使用表格展示支持的操作及其时间复杂度

**写作风格**：

- 简洁直接，避免冗长的理论解释
- 使用加粗强调关键术语
- 适当使用代码格式标记索引关系、公式等

**示例参考**：

```markdown
## 什么是堆？

堆是一种特殊的**完全二叉树**数据结构，满足堆性质：

- **最小堆（Min Heap）**：父节点的值 ≤ 子节点的值，堆顶是最小元素
- **最大堆（Max Heap）**：父节点的值 ≥ 子节点的值，堆顶是最大元素

### 核心性质

1. **完全二叉树**：除了最后一层，其他层都是满的，最后一层从左到右填充
2. **数组存储**：使用数组实现，索引关系如下：
    - 节点 `i` 的左子节点：`2*i + 1`
    - 节点 `i` 的右子节点：`2*i + 2`
    - 节点 `i` 的父节点：`(i - 1) // 2`

### 支持的操作

| 操作        | 时间复杂度    | 说明          |
|-----------|----------|-------------|
| `heapify` | O(n)     | 从数组建堆       |
| `push`    | O(log n) | 插入元素        |
| `pop`     | O(log n) | 弹出堆顶元素      |
```

#### 第二部分：[算法]适合解决什么问题？

**目标**：帮助读者判断何时使用该算法

**内容要求**：

- 列举 3-5 个典型应用场景
- 每个场景包含：
    - 场景名称（加粗）
    - 简短描述
    - 时间复杂度优势说明
- 使用编号列表组织

**写作风格**：

- 从实际问题出发，而非抽象概念
- 强调相比其他方法的优势
- 给出具体的复杂度对比

**示例参考**：

```markdown
## 堆适合解决什么问题？

堆特别适合处理以下场景：

1. **Top K 问题**：找最大/最小的 K 个元素
    - 时间复杂度：O(n log k)，优于排序的 O(n log n)

2. **动态维护最值**：需要频繁插入元素并获取最值
    - 例如：实时获取中位数、滑动窗口最值

3. **优先级队列**：按优先级处理任务
    - 例如：任务调度、Dijkstra 最短路径
```

#### 推荐学习资料（必需）

**目标**：给读者补一组可以继续延伸阅读的中文资料，兼顾概念理解、题目套路和实现细节

**内容要求**：

- 必须单独使用 `## 推荐学习资料` 二级标题
- 放在“适合解决什么问题？”之后、“笔试场景建议”之前
- 先用 1 句话说明推荐阅读顺序或阅读目的
- 提供 **2-3 个中文资料链接**
- 每个链接后面都要附一小句说明，解释这份资料适合补什么

**链接选择要求**：

- 优先选择和当前算法主题直接相关的中文教程、题解总结或专题页面
- 优先来源：**Hello 算法**、**OI Wiki**、**代码随想录**、**牛客博客**、**力扣中文讨论 / 题解**、**洛谷专栏 / 题单** 等
- 不要只丢网站首页凑数；如果没有更合适的专题页，才退而求其次使用相关栏目页
- 尽量按“**概念入门 -> 题目套路 -> 进阶 / 实现细节**”的顺序排列

**示例参考**：

```markdown
## 推荐学习资料

如果你想系统学习这个算法，建议按下面顺序看这些中文资料：

- [Hello 算法：堆](https://www.hello-algo.com/chapter_heap/heap/) - 图示和基础操作讲得清楚，适合先把结构和操作过一遍
- [Hello 算法：建堆操作](https://www.hello-algo.com/chapter_heap/build_heap/) - 适合补 `heapify` 和 O(n) 建堆的理解
- [OI Wiki：堆](https://oiwiki.org/ds/heap/) - 更偏模板和实现细节，适合拿来对照代码
```

#### 第三部分：笔试场景建议

**目标**：给出明确的策略建议

**内容要求**：

- 使用 "推荐策略" 子标题
- 用编号列表列出不同方案
- 每个方案使用 ✅ 或 ❌ 标记优缺点
- 明确指出推荐顺序

**写作风格**：

- 直接给出建议，不要模棱两可
- 使用对比的方式突出差异
- 考虑笔试时间压力和容错性

**示例参考**：

```markdown
## 笔试场景建议

### 推荐策略

1. **优先使用 `heapq`**（Python 标准库）
    - ✅ 省时省力，代码简洁
    - ✅ 经过优化，性能更好
    - ✅ 不容易出错

2. **手动实现的场景**
    - ❌ 题目明确禁止使用标准库
    - ❌ 需要特殊的堆操作（如修改堆中元素）
```

#### 第四部分：方案一 - 使用标准库（如适用）

**目标**：提供标准库的完整使用指南

**内容要求**：

- 基本用法（最简单的示例）
- 常见技巧（3-5 个实用技巧）
- 每个技巧包含：
    - 小标题（使用 ### 三级标题）
    - 问题描述或使用场景
    - 完整的代码示例
    - 关键注释

**写作风格**：

- 代码示例要完整可运行
- 注释要精炼，只解释关键点
- 按使用频率排序（最常用的放前面）
- 代码块使用 ```python 标记

**示例参考**：

```markdown
## 方案一：使用 heapq（推荐）

### 基本用法

```python
import heapq

# 最小堆
heap = []
heapq.heappush(heap, 3)  # 插入元素
min_val = heapq.heappop(heap)  # 弹出最小值
```

### 最大堆实现

```python
# 方法1: 存储负数
max_heap = []
heapq.heappush(max_heap, -3)
max_val = -heapq.heappop(max_heap)  # 弹出最大值: 3

# 方法2: 存储 (-x, x) 元组（推荐）
max_heap = []
heapq.heappush(max_heap, (-3, 3))
_, max_val = heapq.heappop(max_heap)
```
```

#### 第五部分：方案二 - 手动实现

**目标**：提供易于记忆的手写实现

**内容要求**：

- 使用 "适合速记的[算法]实现" 作为小标题
- 提供精简的类实现（不超过 50 行）
- 只包含核心方法（push、pop、peek）
- 每个方法标注时间复杂度
- 关键步骤添加注释
- 提供使用示例

**写作风格**：

- 代码要简洁，优先考虑可读性
- 变量名要简短（如 h、i、p）
- 注释要说明"为什么"而非"是什么"
- 避免过度优化

**示例参考**：

```markdown
## 方案二：手动实现堆

### 适合速记的最小堆实现

```python
class Heap:
    def __init__(self):
        self.h = []

    def push(self, x):
        """插入元素 - O(log n)"""
        self.h.append(x)
        i = len(self.h) - 1
        # 向上调整（上浮）
        while i > 0:
            p = (i - 1) // 2  # 父节点索引
            if self.h[i] < self.h[p]:
                self.h[i], self.h[p] = self.h[p], self.h[i]
                i = p
            else:
                break
```

### 使用示例

```python
# 最小堆
heap = Heap()
heap.push(3)
print(heap.pop())  # 1

# 最大堆：存储负数
max_heap = Heap()
max_heap.push(-3)
print(-max_heap.pop())  # 3
```

```

#### 第六部分：常见题型速查

**目标**：提供快速参考的代码模板

**内容要求**：
- 列举 3-5 个最常见的题型
- 每个题型使用 ### 三级标题
- 提供核心代码片段（10-20 行）
- 代码要通用，可直接套用
- 添加简短的说明注释

**写作风格**：
- 代码要精炼，去掉无关细节
- 突出算法核心逻辑
- 使用 # 处理节点... 等注释表示省略部分

**示例参考**：
```markdown
## 常见题型速查

### 1. Top K 问题

```python
# 找最大的 k 个元素：维护大小为 k 的最小堆
heap = []
for num in nums:
    if len(heap) < k:
        heapq.heappush(heap, num)
    elif num > heap[0]:
        heapq.heappop(heap)
        heapq.heappush(heap, num)
```

### 2. 合并 K 个有序链表

```python
heap = []
for i, lst in enumerate(lists):
    if lst:
        heapq.heappush(heap, (lst.val, i, lst))

while heap:
    val, i, node = heapq.heappop(heap)
    # 处理节点...
```

```

#### 第七部分：实战技巧总结

**目标**：提供快速记忆的要点清单

**内容要求**：
- 使用 ✅ 标记每个要点
- 每个要点一行，简洁明了
- 5-8 个要点
- 按重要性排序

**写作风格**：
- 使用祈使句（"使用..."、"避免..."）
- 突出对比（"用 X 而非 Y"）
- 包含性能提示

**示例参考**：
```markdown
## 实战技巧总结

✅ **最小堆**：直接使用 `heapq` 或 `Heap()`

✅ **最大堆**：存储 `(-x, x)` 或 `-x`

✅ **自定义排序**：使用 `(priority, data)` 元组

✅ **避免对象比较**：使用 `(priority, counter, object)`

✅ **从列表建堆**：`heapify()` 比逐个 `push` 更快（O(n) vs O(n log n)）
```

#### 第八部分：参考资料

**目标**：引导读者查看完整代码

**内容要求**：

- 使用 markdown 链接格式
- 每个文件一行，附带简短说明
- 可选：添加"推荐阅读顺序"

**示例参考**：

```markdown
## 参考资料

- [heapq_native.py](heapq_native.py) - 使用 heapq 的完整示例
- [heap_manual.py](heap_manual.py) - 手动实现堆的完整代码

**推荐阅读顺序**：

1. 先看本 README 了解基本概念和策略
2. 参考 `heapq_native.py` 学习 heapq 用法
3. 需要手写时，参考 `heap_manual.py` 中的速记模板
```

---

### 通用写作原则

1. **语言风格**
    - 使用简洁、直接的中文
    - 避免过于学术化的表述
    - 多用主动语态和祈使句
    - 适当使用口语化表达（如"这玩意"、"省时省力"）

2. **格式规范**
    - 一级标题：`#` 仅用于文件标题
    - 二级标题：`##` 用于主要章节
    - 三级标题：`###` 用于子章节和具体技巧
    - 代码块必须指定语言：```python
    - 使用表格展示结构化数据（如时间复杂度）

3. **代码示例规范**
    - 所有代码必须可直接运行
    - 使用有意义但简短的变量名
    - 关键步骤添加行内注释
    - 避免过长的代码块（超过 30 行考虑拆分）

4. **强调和标记**
    - 使用 **加粗** 强调关键术语和概念
    - 使用 `代码格式` 标记函数名、变量名、操作名
    - 使用 ✅ 表示推荐做法
    - 使用 ❌ 表示不推荐或特殊场景

5. **实用性优先**
    - 从实际问题出发，而非理论
    - 提供可直接套用的代码模板
    - 说明时间复杂度和性能考虑
    - 给出明确的使用建议
6. **推荐学习资料**
    - 每个算法 README 都必须包含 `## 推荐学习资料`
    - 至少提供 2-3 个中文资料链接，并附一句用途说明
    - 尽量覆盖“概念入门 / 题目套路 / 模板细节”三个层次

### 3. Python 代码文件规范

- **文件头注释**：说明模板用途、支持的 Python 版本、核心原理
- **代码风格**：简洁易记，充分利用 Python 特性
- **注释**：关键步骤添加注释，说明时间复杂度和原理
- **示例代码**：在 `if __name__ == '__main__':` 中提供完整的使用示例
- **兼容性**：确保代码在 Python 3.7-3.14 上都能运行

### 4. 命名规范

- 目录名：使用小写字母和下划线（如 `heap`、`binary_search`）
- Python 文件名：描述性命名（如 `heapq_native.py`、`heap_manual.py`）
- README.md：固定名称，必须大写

## 常用命令

### 运行算法示例

```bash
# 运行特定算法的示例代码
python algos/heap/heapq_native.py
python algos/heap/heap_manual.py
```

### 测试代码兼容性

```bash
# 使用不同 Python 版本测试
python3.7 algos/heap/heapq_native.py
python3.8 algos/heap/heapq_native.py
python3.12 algos/heap/heapq_native.py
```

### 配置 tools/ 环境

`algos/` 目录下的模板仍然坚持纯标准库，但 `tools/` 里的本地分析工具可以使用第三方依赖。

当前 `tools/interview_complexity.py` 依赖 `complexipy` 和 `radon`，因此需要单独安装依赖，且运行环境要求 **Python 3.8+**。

```bash
# 推荐：单独准备一个虚拟环境
python3.12 -m venv .venv
source .venv/bin/activate

# 安装工具依赖
python -m pip install --upgrade pip
python -m pip install -r requirements.txt
```

### 使用面试手写复杂度工具

```bash
# 分析整个文件，默认输出每个函数/方法的摘要
python -m tools.interview_complexity -i algos/heap/heap_manual.py

# 精查某个符号（函数 / 方法 / 类）
python -m tools.interview_complexity -i algos/heap/heap_manual.py -s Heap._sift_down

# 输出 JSON，方便后续脚本接入
python -m tools.interview_complexity -i algos/heap/heap_manual.py -s Heap.push --json

# 如果你明确想把 __main__ / demo / example 这类示例代码也算进去
python -m tools.interview_complexity -i algos/heap/heapq_native.py --include-examples

# 直接分析一段代码字符串
python -m tools.interview_complexity -c "for i in range(n):\n    if a[i] > 0:\n        ans += 1"
```

使用说明：

- 默认会**严格聚焦核心代码**：优先分析函数 / 方法 / 类，不把 `if __name__ == '__main__'`、`demo` / `example` / `test` 命名的示例代码纳入摘要
- 如果你确实要分析示例代码，可以显式加 `--include-examples`
- 使用 `-s/--symbol` 可以对单个符号输出完整报告
- 评分越高，越偏向“现场手写容易出错”；等级分为“很容易手写 / 正常可写 / 坑比较多 / 现场高风险”
- 这个工具是为了衡量**面试手写体感**，不是严格的学术复杂度评估器；它会结合 `cognitive complexity`、`cyclomatic complexity` 和一些 AST 风险规则一起打分

### AI / Agent 使用约定

当 AI agent 需要评估一段实现的“面试手写复杂度”时，遵循以下约定：

- **优先使用** `python -m tools.interview_complexity -c "..."`，而不是直接把整个文件路径丢给 `-i`
- 传给 `-c` 的内容必须尽量只保留**核心代码**
  - 例如：核心函数、核心方法、核心循环、核心状态转移逻辑
  - 不要把 `if __name__ == '__main__'`、示例输入输出、print 调试、usage/demo/example/test 代码一并传进去
- 如果文件里既有实现又有大量演示代码，先提取实现片段，再用 `-c` 分析
- 只有在以下场景才优先考虑 `-i`
  - 需要快速扫描整份实现文件，找出最复杂的函数/方法
  - 代码已经天然按函数/方法拆得很干净，文件里几乎没有示例部分
- 如果只是想比较两种写法谁更适合面试现场手写，优先把两段**最小核心实现**分别传给 `-c`，不要让外围样板代码干扰结果

## 开发注意事项

1. **纯原生库**：不要引入第三方依赖，确保 OJ 平台兼容性
   - 该要求严格适用于 `algos/` 下的算法模板
   - `tools/` 下的本地分析工具允许使用第三方依赖，但必须写入根目录 `requirements.txt`
2. **版本兼容**：避免使用 Python 3.8+ 专属特性（如海象运算符 `:=`）
3. **性能优化**：针对 Python 的性能特点优化，避免常见陷阱
4. **代码简洁**：优先考虑可读性和可记忆性，而非过度优化
5. **实战验证**：所有模板应在实际算法题中验证过

## 目标读者

- 有 Python 基础，熟悉常用语法和数据结构
- 了解基本算法概念
- 准备用 Python 参加笔试/面试
- 想快速掌握 Python 算法模板

## 许可证

Apache 2.0
